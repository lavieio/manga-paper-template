# ── 项目私有材料（不入 git）─────────────────────────
# 页面原型（设计定稿基准，仅本地）
demos/
# 实现方案等内部文档
docs/
# AI 开发规范（本仓专用，不公开）
.agents/
AGENTS.md
# 本地 ticket tracker（技能工作区）
.scratch/
# 本地开发工具（单元测试 / 产物断言 / 浏览器冒烟）：只在本地跑，不进仓
devtools/

# ── 环境变量（.env.example 必须保留）──────────────
.env
.env.local
.env.*.local
!.env.example

# ── 构建产物 ────────────────────────────────────────
dist/
pagefind/

# ── 依赖 ────────────────────────────────────────────
node_modules/

# ── Astro 缓存 ─────────────────────────────────────
.astro/

# ── 日志 ────────────────────────────────────────────
*.log
logs/
npm-debug.log*

# ── 编辑器 / 系统 ──────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db
