# MangaPaper 环境变量示例。复制为 .env 并填入真实值（.env 不进 git）。

# 私密文章加密密码（构建期读取，不进页面）。
# 默认 "manga-paper"——仅适合本地预览，上线必须改成强密码（建议 12+ 位或长句）。
PRIVATE_PASSWORD=manga-paper

# 私密解锁记忆时长（小时），浏览器端生效，默认 1。
PUBLIC_UNLOCK_TTL_HOURS=1

# 站点根 URL（驱动 canonical / sitemap / RSS）。
SITE_URL=https://your-domain.com

# remark42 评论（可选）：两项都配齐才会在文章页渲染评论区，未配置则整块不显示。
# PUBLIC_REMARK42_HOST=https://comments.example.com
# PUBLIC_REMARK42_SITE_ID=manga-paper
